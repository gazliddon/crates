[package]
name = "emuz80"
version = "0.1.0"
edition = "2021"
description = "Z80 instruction set database (shared by the gazm assembler backend and, later, an emulator)"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0.188", features = ["derive"] }
serde_json = "1.0.107"
